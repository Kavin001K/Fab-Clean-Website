{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm --filter \"@workspace/api-server\" build",
    "build:all": "pnpm -r --if-present run build",
    "build:backend": "pnpm --filter \"@workspace/api-server\" build",
    "start": "node Backend/dist/index.cjs",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./frontend\" --filter \"./backend\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "eslint": "^10.2.1",
    "framer-motion": "^12.38.0"
  },
  "description": "pnpm workspace monorepo using TypeScript. Each package manages its own dependencies.",
  "directories": {
    "lib": "lib"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Kavin001K/Fab-Clean-Website.git"
  },
  "keywords": [],
  "author": "",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/Kavin001K/Fab-Clean-Website/issues"
  },
  "homepage": "https://github.com/Kavin001K/Fab-Clean-Website#readme"
}
